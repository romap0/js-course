/**
 * Задание 4: Пересечение двух массивов
 *
 * Верни массив элементов, которые есть в обоих массивах.
 * Результат должен содержать только уникальные элементы.
 *
 * @param {Array} a
 * @param {Array} b
 * @returns {Array}
 *
 * @example
 * intersection([1, 2, 3], [2, 3, 4])     // [2, 3]
 * intersection([1, 1, 2], [2, 2, 3])     // [2]
 * intersection([1, 2], [3, 4])           // []
 */
export function intersection(a, b) {
  // Твой код здесь
}
