/**
 * Задание 6: Плоский массив (flatten)
 *
 * Верни «плоский» массив из вложенного.
 * НЕ используй Array.flat — напиши рекурсию сам.
 *
 * @param {Array} arr — массив с произвольной вложенностью
 * @returns {Array} одномерный массив
 *
 * @example
 * flatten([1, [2, [3, [4]]]])  // [1, 2, 3, 4]
 * flatten([[1, 2], [3, 4]])    // [1, 2, 3, 4]
 * flatten([1, 2, 3])           // [1, 2, 3]
 */
export function flatten(arr) {
  // Твой код здесь
}
