/**
 * Задание 3: Группировка
 *
 * Сгруппируй элементы массива по результату вызова функции fn.
 * Верни объект, где ключи — результаты fn,
 * а значения — массивы элементов с таким результатом.
 *
 * @param {Array} arr
 * @param {Function} fn — функция, возвращающая ключ группы
 * @returns {Object}
 *
 * @example
 * groupBy([1, 2, 3, 4, 5], x => x % 2 === 0 ? "even" : "odd")
 * // { odd: [1, 3, 5], even: [2, 4] }
 *
 * groupBy(["one", "two", "three"], s => s.length)
 * // { 3: ["one", "two"], 5: ["three"] }
 */
export function groupBy(arr, fn) {
  // Твой код здесь
}
