/**
 * Задание 2: Уникальные элементы
 *
 * Верни новый массив, содержащий только уникальные элементы.
 * Порядок первого появления должен сохраняться.
 *
 * @param {Array} arr
 * @returns {Array}
 *
 * @example
 * unique([1, 2, 2, 3, 1]) // [1, 2, 3]
 * unique(["a", "b", "a"]) // ["a", "b"]
 * unique([])               // []
 */
export function unique(arr) {
  // Твой код здесь
}
