/**
 * Задание 5: Каррирование сложения
 *
 * Верни функцию, которая запоминает первый аргумент
 * и при вызове прибавляет к нему второй.
 *
 * @param {number} a
 * @returns {Function} функция, принимающая b и возвращающая a + b
 *
 * @example
 * const add5 = createAdder(5);
 * add5(3)  // 8
 * add5(10) // 15
 */
export function createAdder(a) {
  // Твой код здесь
}
