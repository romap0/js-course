/**
 * Задание 1: Возведение в степень
 *
 * Верни base в степени exp.
 * НЕ используй Math.pow и оператор ** — напиши цикл.
 * exp всегда >= 0.
 *
 * @param {number} base — основание
 * @param {number} exp — показатель степени (неотрицательное целое)
 * @returns {number}
 *
 * @example
 * power(2, 3)  // 8
 * power(5, 0)  // 1
 * power(3, 2)  // 9
 */
export function power(base, exp) {
  // Твой код здесь
}
