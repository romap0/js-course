/**
 * Задание 2: Подсчёт символов
 *
 * Верни количество вхождений символа char в строке str.
 *
 * @param {string} str — строка
 * @param {string} char — один символ
 * @returns {number}
 *
 * @example
 * countChar("hello", "l")  // 2
 * countChar("hello", "z")  // 0
 * countChar("aaa", "a")    // 3
 */
export function countChar(str, char) {
  // Твой код здесь
}
