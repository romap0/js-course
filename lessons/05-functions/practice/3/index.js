/**
 * Задание 3: Применение функции к каждому элементу
 *
 * Верни новый массив, в котором к каждому элементу
 * применена функция fn.
 * НЕ используй Array.map — напиши цикл.
 *
 * @param {Array} arr — исходный массив
 * @param {Function} fn — функция преобразования
 * @returns {Array}
 *
 * @example
 * myMap([1, 2, 3], x => x * 2) // [2, 4, 6]
 * myMap(["a", "b"], s => s.toUpperCase()) // ["A", "B"]
 */
export function myMap(arr, fn) {
  // Твой код здесь
}
