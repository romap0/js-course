/**
 * Задание 2: Оценка по баллам
 *
 * Верни буквенную оценку по числовому баллу:
 *   90–100 → "A"
 *   70–89  → "B"
 *   50–69  → "C"
 *   30–49  → "D"
 *   0–29   → "F"
 *
 * @param {number} score — балл от 0 до 100
 * @returns {string} оценка
 *
 * @example
 * getGrade(95) // "A"
 * getGrade(72) // "B"
 * getGrade(45) // "D"
 */
export function getGrade(score) {
  // Твой код здесь
}
