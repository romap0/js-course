/**
 * Задание 1: Модуль числа
 *
 * Верни абсолютное значение числа (модуль).
 * НЕ используй Math.abs — напиши логику сам через if.
 *
 * @param {number} n
 * @returns {number}
 *
 * @example
 * getAbsoluteValue(5)   // 5
 * getAbsoluteValue(-3)  // 3
 * getAbsoluteValue(0)   // 0
 */
export function getAbsoluteValue(n) {
  // Твой код здесь
}
