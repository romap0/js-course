/**
 * Задание 6: Цена билета
 *
 * Верни цену билета в зависимости от возраста:
 *   0–6    → 0    (бесплатно)
 *   7–17   → 50   (детский)
 *   18–64  → 100  (взрослый)
 *   65+    → 40   (льготный)
 *
 * @param {number} age — возраст
 * @returns {number} цена билета
 *
 * @example
 * getTicketPrice(5)   // 0
 * getTicketPrice(12)  // 50
 * getTicketPrice(30)  // 100
 * getTicketPrice(70)  // 40
 */
export function getTicketPrice(age) {
  // Твой код здесь
}
