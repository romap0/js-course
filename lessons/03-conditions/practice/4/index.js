/**
 * Задание 4: Право голоса
 *
 * Верни true, если человек может голосовать:
 * возраст >= 18 И есть гражданство (hasCitizenship === true).
 *
 * @param {number} age
 * @param {boolean} hasCitizenship
 * @returns {boolean}
 *
 * @example
 * canVote(20, true)   // true
 * canVote(20, false)  // false
 * canVote(16, true)   // false
 */
export function canVote(age, hasCitizenship) {
  // Твой код здесь
}
