/**
 * Задание 5: Форматирование цены
 *
 * Если цена === 0, верни "Бесплатно".
 * Иначе верни строку в формате "{цена} ₽".
 *
 * Попробуй использовать тернарный оператор.
 *
 * @param {number} price
 * @returns {string}
 *
 * @example
 * formatPrice(0)   // "Бесплатно"
 * formatPrice(100) // "100 ₽"
 */
export function formatPrice(price) {
  // Твой код здесь
}
