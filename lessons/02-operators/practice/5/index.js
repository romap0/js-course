/**
 * Задание 5: Число в диапазоне?
 *
 * Верни true, если num находится в диапазоне от min до max включительно.
 *
 * @param {number} num
 * @param {number} min
 * @param {number} max
 * @returns {boolean}
 *
 * @example
 * isInRange(5, 1, 10)  // true
 * isInRange(0, 1, 10)  // false
 * isInRange(10, 1, 10) // true
 */
export function isInRange(num, min, max) {
  // Твой код здесь
}
