/**
 * Задание 6: Значение по умолчанию (nullish coalescing)
 *
 * Верни value, если оно не null и не undefined.
 * Иначе верни defaultVal.
 * Используй оператор ??.
 *
 * @param {*} value
 * @param {*} defaultVal
 * @returns {*}
 *
 * @example
 * getDefault(null, 5)       // 5
 * getDefault(undefined, 5)  // 5
 * getDefault(0, 5)          // 0
 */
export function getDefault(value, defaultVal) {
  // Твой код здесь
}
