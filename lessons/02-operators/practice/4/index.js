/**
 * Задание 4: Расчёт скидки
 *
 * Верни цену со скидкой.
 * Например, если цена 200 и скидка 10%, результат — 180.
 *
 * @param {number} price — исходная цена
 * @param {number} percent — процент скидки (от 0 до 100)
 * @returns {number} цена со скидкой
 *
 * @example
 * getDiscount(200, 10) // 180
 * getDiscount(100, 25) // 75
 */
export function getDiscount(price, percent) {
  // Твой код здесь
}
