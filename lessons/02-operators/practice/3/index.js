/**
 * Задание 3: Чётное ли число?
 *
 * Верни true, если число чётное, иначе false.
 * Подсказка: используй оператор остатка от деления (%).
 *
 * @param {number} num
 * @returns {boolean}
 *
 * @example
 * isEven(4) // true
 * isEven(7) // false
 */
export function isEven(num) {
  // Твой код здесь
}
