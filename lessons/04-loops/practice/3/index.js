/**
 * Задание 3: Разворот строки
 *
 * Верни строку, записанную задом наперёд.
 * Используй цикл (НЕ используй Array.reverse).
 *
 * @param {string} str
 * @returns {string}
 *
 * @example
 * reverseString("hello")  // "olleh"
 * reverseString("abc")    // "cba"
 * reverseString("")       // ""
 */
export function reverseString(str) {
  // Твой код здесь
}
