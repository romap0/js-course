/**
 * Задание 6: Степень двойки
 *
 * Верни true, если число является степенью двойки (1, 2, 4, 8, 16, ...).
 * Используй цикл while.
 *
 * Подсказка: делите число на 2, пока оно чётное.
 * Если в конце осталась 1 — это степень двойки.
 *
 * @param {number} n — положительное целое число
 * @returns {boolean}
 *
 * @example
 * isPowerOfTwo(8)  // true  (2^3)
 * isPowerOfTwo(10) // false
 * isPowerOfTwo(1)  // true  (2^0)
 */
export function isPowerOfTwo(n) {
  // Твой код здесь
}
