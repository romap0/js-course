/**
 * Задание 5: Поиск элемента
 *
 * Верни индекс первого вхождения элемента target в массиве.
 * Если элемент не найден, верни -1.
 * НЕ используй Array.indexOf — напиши цикл сам.
 *
 * @param {Array} arr — массив
 * @param {*} target — искомый элемент
 * @returns {number} индекс или -1
 *
 * @example
 * findIndex([1, 2, 3], 2)       // 1
 * findIndex(["a", "b", "c"], "c") // 2
 * findIndex([1, 2, 3], 5)       // -1
 */
export function findIndex(arr, target) {
  // Твой код здесь
}
