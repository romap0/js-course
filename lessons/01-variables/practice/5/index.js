/**
 * Задание 5: Создание сообщения
 *
 * Верни строку в формате "{name} — {age} лет"
 * Используй шаблонные литералы.
 *
 * @param {string} name — имя
 * @param {number} age — возраст
 * @returns {string}
 *
 * @example
 * createMessage("Иван", 25) // "Иван — 25 лет"
 */
export function createMessage(name, age) {
  // Твой код здесь
}
