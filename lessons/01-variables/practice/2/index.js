/**
 * Задание 2: Определение типа
 *
 * Верни строку с типом переданного значения.
 * Используй оператор typeof.
 *
 * @param {*} value — любое значение
 * @returns {string} тип значения
 *
 * @example
 * getType(42)        // "number"
 * getType("hello")   // "string"
 */
export function getType(value) {
  // Твой код здесь
}
