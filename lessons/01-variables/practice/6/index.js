/**
 * Задание 6: Значение по умолчанию
 *
 * Если value равно undefined или null, верни defaultValue.
 * Иначе верни value.
 *
 * Подсказка: используй оператор ?? (nullish coalescing).
 *
 * @param {*} value — значение
 * @param {*} defaultValue — значение по умолчанию
 * @returns {*}
 *
 * @example
 * getValueOrDefault(undefined, 10)  // 10
 * getValueOrDefault(null, "тест")   // "тест"
 * getValueOrDefault(0, 10)          // 0
 * getValueOrDefault("", "default")  // ""
 */
export function getValueOrDefault(value, defaultValue) {
  // Твой код здесь
}
