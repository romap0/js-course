/**
 * Задание 4: Преобразование в число
 *
 * Преобразуй строку в число и верни результат.
 * Используй функцию Number().
 *
 * @param {string} str — строка для преобразования
 * @returns {number}
 *
 * @example
 * convertToNumber("42")    // 42
 * convertToNumber("3.14")  // 3.14
 * convertToNumber("hello") // NaN
 */
export function convertToNumber(str) {
  // Твой код здесь
}
