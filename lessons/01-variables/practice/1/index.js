/**
 * Задание 1: Приветствие
 *
 * Верни строку приветствия в формате "Привет, {name}!"
 * Используй шаблонные литералы (обратные кавычки).
 *
 * @param {string} name — имя пользователя
 * @returns {string} строка приветствия
 *
 * @example
 * getGreeting("Анна") // "Привет, Анна!"
 */
export function getGreeting(name) {
  // Твой код здесь
}
