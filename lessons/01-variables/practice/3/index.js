/**
 * Задание 3: Совершеннолетие
 *
 * Верни true, если возраст >= 18, иначе false.
 *
 * @param {number} age — возраст
 * @returns {boolean}
 *
 * @example
 * isAdult(20)  // true
 * isAdult(15)  // false
 */
export function isAdult(age) {
  // Твой код здесь
}
